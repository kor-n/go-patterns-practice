// Package fanin: слить несколько каналов в один.
//
// Merge получает несколько входных каналов и возвращает один выходной,
// в который попадают все значения из входных.
//
// Требования:
//   - ни одно значение не теряется и не дублируется;
//   - каналы читаются одновременно, а не по очереди;
//   - выходной канал закрывается, когда закрылись все входные;
//   - без входных каналов возвращается закрытый канал;
//   - отмена контекста закрывает выходной канал, даже если один из входных
//     завис: в него никто не пишет и никто его не закроет;
//   - если потребитель перестал читать и отменил контекст, внутренние
//     горутины завершаются.
//
// Пример:
//
//	a := producer(1, 2) // канал, отдающий 1 и 2
//	b := producer(3)    // канал, отдающий 3
//
//	for v := range Merge(ctx, a, b) {
//		fmt.Println(v) // 1, 2 и 3 в произвольном порядке
//	}
package fanin

import "context"

// Merge сливает значения из всех входных каналов в один выходной.
func Merge[T any](ctx context.Context, chs ...<-chan T) <-chan T {
	panic("не реализовано")
}
